
// Palabras reservadas
// utilizarlas en la declaracion
// de los tipos o nombres de funciones que se quieren utilizar en el emisor de eventos

module.exports = {
    events: {
        GREET: 'greet'
    }
}